# 2026-03-19

## 完成：为所有页面添加用户菜单

- 将 `create.html` 中已有的用户下拉菜单（头像点击 → 显示登录/退出）复制到其余 5 个页面：
  - `media.html`、`result.html`、`translate-v6.html`、`livestream.html`、`generate.html`
- 每个页面新增内容：
  1. CSS：`.user-dropdown` 系列样式（~80 行，内联 `<style>`）
  2. HTML：头像 div 加 id + 完整下拉菜单 DOM
  3. JS：IIFE 用户逻辑（localStorage 读写、登录跳转、退出登录、跨标签页 storage 同步）
- 已提交推送：`feat: 为所有页面添加侧边栏用户菜单（登录/退出/状态同步）`
- MEMORY.md 待办事项已标记完成

## 完成：首页改造为 AI 统一入口

- 将 `create.html` 从三张功能卡片导航升级为类似 ChatGPT 的统一意图入口
- 新增 Skill 选择器（视频译制/直播剪辑/AI 生成）3 个胶囊按钮
- 新增统一输入框，支持文本输入 + 文件上传 + 链接粘贴
- 每个 Skill 对应 4 条快捷提示词（点击自动填入）
- 可折叠的高级配置面板，根据 Skill 动态切换配置项
- 简易意图识别：根据用户输入关键词自动匹配 Skill
- 提交后携带 prompt 和配置参数跳转到对应功能页面
- Phase 2 预留：沉浸式配置页输入框 + 多 Skill 串联（后续做）

## 完成：输入框改造 — Action Slot + Config Capsules

- 将 `create.html` 的输入框从简单 textarea 升级为「任务卡」结构
- **Action Slot**（textarea 上方，根据 Skill 动态切换）：
  - 视频译制：拖拽/点击上传视频区，支持拖放、文件预览、删除
  - 直播剪辑：专用链接粘贴栏，带一键粘贴按钮、平台自动识别（抖音/快手/B站等）
  - AI 生成：参考图上传卡片（可选），上传后缩略图预览
- **Config Capsules**（textarea 下方、工具栏上方）：
  - 每个高级配置项变成一个胶囊标签，如 `[英语▾]` `[自动匹配▾]`
  - 点击弹出 inline dropdown 选择，无需折叠面板
  - 用户修改过的胶囊高亮显示（跟随 Skill 主色：绿/橙/紫）
- 移除了旧的「高级配置」折叠按钮和面板
- 工具栏的上传/链接按钮作为备用入口，智能关联 Action Slot

## 完成：Action Slot → Attach Bar 架构迁移收尾

- 修复 `uploadBtn` click handler：移除对已删除的 `renderUploadPreview()`、`renderRefPreview()`、`uploadZone`、`refUploadZone` 的引用，统一改用 `renderAttachBar()`
- 修复 `linkBtn` click handler：移除对已删除的 `linkBarInput` 元素和 `renderLinkBar()` 的引用，统一改用 `renderAttachBar()`
- 现在三个 Skill 模式下上传/链接流程完整可用，无运行时报错

## 完成：主页重写为 Leonardo AI 风格

- 完全重写 `create.html`，参考 Leonardo AI 的设计风格
- **新布局**：
  - Hero 区域：浮动星形图标 + 标题「你想创作什么？」
  - 三张大卡片：视频译制（绿色调）/ 直播剪辑（橙色调）/ AI 生成（紫色调），每张有渐变背景 + emoji 装饰 + 标题描述
  - 底部简洁输入框：仅 textarea + 上传/链接按钮 + 发送按钮
- **Skill 实时识别浮出**：用户在输入框打字时，根据关键词实时检测意图，在输入框上方浮出对应 Skill 标签（带动画），点击标签即跳转
- **移除的模块**：Skill 选择器胶囊、Config Capsules 配置胶囊、快捷提示词 chips、底部能力标签
- 卡片点击直接跳转到对应功能页面
- 上传/链接附件以 attach-tag 形式显示

## 完成：视频译制子功能意图识别（收尾）

- 上次会话已完成大部分工作：子功能定义（subModes）、detectTranslateSubMode()、浮出标签双标签显示、卡片点击传 mode
- 本次修复了遗留的两个函数：
  - `handleSubmit()` → 传递 `detectedSubMode` 给 `goToSkill()`
  - `goToSkill(sk)` → `goToSkill(sk, mode)` 支持 mode 参数，当 `sk === 'translate' && m` 时 append `mode=` 到 URL
- 现在输入"帮我擦字幕"→ 检测到视频译制 + 擦除字幕子功能 → 浮出两个标签 → 跳转 `translate-v6.html?mode=erase`

## 完成：首页背景光效 + 智能引导 + 直接跳转

### 1. 背景氛围光增强
- `.create-home::before` / `::after` 添加两层固定氛围光球，带漂浮呼吸动画（`ambientDrift` / `ambientDrift2`）
- 卡片底部氛围光 / 顶部柔光线默认微弱可见（opacity 0.45/0.35），不仅仅是 hover 才出现
- 卡片背景从 `0.02` → `0.03`，加 `inset` 内发光和 `box-shadow`
- 图标 opacity 从 0.35 → 0.5
- 输入框加默认 `box-shadow` 和微弱氛围光

### 2. 提交直接跳转功能页
- 点击发送后 `goToSkill()` 直接 `window.location.href` 到对应功能页（translate-v6/livestream/generate），不再经过 landing

### 3. 智能素材引导
- 识别到 `translate` 但没上传视频 → 弹出 nudge toast「请先上传一个视频文件」+ 上传按钮脉冲高亮
- 识别到 `livestream` 但没添加链接 → 弹出 nudge toast「请先添加直播链接」+ 链接按钮脉冲高亮
- `generate` 只需要文案不需要额外素材，直接跳转
- nudge toast 带 shake 动画 + 5 秒自动消失 + 点击按钮直接弹出文件选择器/链接输入
- 上传/添加链接成功后自动隐藏 nudge

## 完成：首页卡片改为真实图片插画

- 用 AI 生成三张暗色风格产品插图，保存到 `app/assets/`：
  - `card-translate.png` — 视频播放器 + 多语言字幕流 + AI 声波
  - `card-livestream.png` — LIVE 直播滑雪画面 + 高光标记 + 精彩片段
  - `card-generate.png` — 文字转视频的 AI 管道 + 胶片 + 创意粒子
- 参考即梦截图，重构卡片结构：
  - 新增 `.skill-card-wrap` 外层容器（flex column），`data-skill` 移到这一层
  - `.skill-card` 内部只有 `<img>` + `.skill-card-overlay`（底部渐变遮罩）
  - `.skill-card-info` 在卡片外部下方，包含标题、描述、「开始创作 →」操作
  - 图片 hover 缩放 + 亮度提升，卡片 hover 上浮 + 主色 box-shadow
- 移除旧的 `.skill-card-visual` / `.skill-card-visual-inner` SVG 图标系统
- 描述文案更详细：体现产品卖点
