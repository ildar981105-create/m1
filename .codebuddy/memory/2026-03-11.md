# 2026-03-11

## 项目初始化

- 从 `av-saas` (framex-app) 迁移至独立新项目 `framex-studio`
- 旧项目已冻结归档，不再修改
- GitHub 仓库: `https://github.com/ildar981105-create/framex-studio`
- GitHub Pages: `https://ildar981105-create.github.io/framex-studio/`

## 当前状态

- translate-v5.html: 配置面板全面升级完成
  - 4 个模式（一键译制/擦除字幕/添加字幕/克隆配音）各自有独立的配置组
  - 新增 UI 组件：卡片选择器、单选按钮组、预览图、文件选择器、样式标签切换、对比图、说明文字
  - JS 交互：配置面板折叠展开、模式 tab 联动配置组、卡片选择互斥、单选互斥、toggle 开关、翻译联动字幕类型、样式标签切换
  - 「导入已完成译制任务」按钮已移入上传框内，与「从腾讯云点播选择」并排
  - 删除了原来独立的 tv2-import-section 区域

## 配置面板逻辑修复

- **核心修复**: 新增 `readDefaultConfig()` + `updateStepMetaFromConfig()` 函数，让沉浸式 Chat 流程读取默认配置面板的用户设置（语言、配音类型、擦除模式等），不再全用硬编码
- 修复添加字幕面板：
  - 去掉源语言的 `zh-en`（中英）选项——不是合法源语言
  - 删除多余的"选择视频文件"输入框
  - 字幕来源改为"使用上方提取的字幕 / 上传外部字幕文件"，添加联动显示上传框
  - 翻译开关默认改为 ON，字幕类型初始值同步改为"双语"
- 修复擦除模型 `data-value="region"` 与擦除方式命名冲突，改为 `data-value="hq"`，标题改为"高质量版"
- CSS: 单选按钮组默认横向排列（`.cfg-radio-group` row），有描述的加 `.vertical` 保持纵向
- CSS: `.tv2-config-cols` gap 从 32px 缩小到 20px
- `showStartButton()` / `runTranslationScan()` / `startTranslationPhase()` 中的语言硬编码全部改为读取 `userConfig`

## 低优先级优化

- 配音选项三层对齐：默认面板（一键译制 + 克隆配音）新增语速/情绪下拉框；精调面板新增情绪下拉框；`updateStepMetaFromConfig` 将语速/情绪同步到 Chat 层配音配置
- 擦除字幕面板："添加擦除区域" toggle 改为"手动添加额外擦除区域"，添加 info-tip 说明
- 一键译制右栏新增独立"字幕翻译目标语言"下拉框（默认"与配音一致"），`readDefaultConfig` 读取后传入字幕步骤

## 导入任务功能增强

- 修复了导入按钮点击无响应的 bug（capturing 阶段 stopPropagation 阻断了事件）
- 导入弹窗新增「通过 ID 导入」模式：
  - 顶部新增 列表浏览 / 通过 ID 导入 切换按钮
  - ID 导入面板：平台选择（云点播/MPS）+ TaskId 输入 + 查询按钮
  - 查询后显示结果卡片（任务名、视频、语言、时长、日期、已完成步骤）
  - 支持 Enter 快捷键触发查询
  - 短 ID（<5位）模拟返回"未找到"的错误状态
  - 查询成功后可点"导入并开始精调"直接进入精调流程

## 导入弹窗层级重构

- **问题**: 导入弹窗层级混乱——有平台tabs(云点播/MPS/FrameX) + mode toggle(列表浏览/ID导入) 两层切换，ID面板里又有独立的平台select，逻辑重复且割裂
- **方案**: 简化为两个 Tab，以 ID 导入为主：
  1. **通过 ID 导入**（默认激活）— 平台下拉(云点播/MPS) + ID 输入 + 查询 + 结果卡片 + 空状态占位图
  2. **FrameX 任务** — 搜索 + 任务列表（仅 FrameX 数据）
- 删除了：旧平台 tabs (vod/mps/framex)、mode toggle 组件、`taskData.vod` / `taskData.mps` 列表数据
- 净减 42 行代码 (145+/187-)

## 精调面板增强 — 内联编辑 + 时间轴拖拽 + 多行重叠

- **内联编辑**: 擦除区域/字幕/配音卡片点击后展开编辑面板，支持修改时间范围(startSec/endSec)、文本、位置/尺寸
- **时间轴拖拽**: 片段左右边缘拖拽调整 start/end 时间，中间拖拽整体移动，带 ±0.5s 吸附
- **多行重叠显示**: `assignLanes()` 算法检测时间重叠片段，分配到不同子行(sublane)显示
- **数据驱动渲染**: 字幕和配音列表从硬编码 HTML 改为 `subtitleItems[]`/`voiceItems[]` 数据驱动
- **双向同步**: `syncAllViews(step)` 统一更新时间轴片段、面板卡片、视频叠加层
- 新增测试数据: E8 顶部水印区域，与其他区域时间重叠，验证多行显示
- 核心函数: `renderSubtitleList`, `renderVoiceList`, `bindSubtitleListEvents`, `bindVoiceListEvents`, `bindEraseInlineEdits`, `syncAllViews`, `syncTimelineSegments`, `createSegmentEl`, `assignLanes`, `snapToNearby`, `selectPanelItem`, `updatePanelAfterDrag`
- commit: ff8ca48

## 精调模式优化 — 单 track 显示 + 擦除编辑解耦

- **单 track 显示**: 进入精调后只显示当前 step 的时间轴 track，其他 track 完全隐藏（不再是 dim 降低透明度）
- **Tab 切换**: 切换精调 tab 时完全替换显示的 track
- **退出恢复**: 退出精调后恢复所有已启用 feature 的 track 可见
- **擦除编辑解耦**: 引入 `editingEraseId` 变量，将「哪个编辑面板展开」与「哪个区域高亮」解耦
  - `activeEraseRegion`: 控制视频画面高亮框 + 卡片 active 样式
  - `editingEraseId`: 控制 inline-edit 编辑面板展开/收起
  - `updateEraseOverlayVisibility()` 时间跟踪只更新高亮，不再干扰编辑面板
- commit: b8fe50f

## Overlay 逻辑清理 — 预览对话状态纯净化

- **核心原则**: overlay（擦除框/字幕预览/配音预览）只在精调模式下显示，对话阶段画面保持纯净
- **删除 `previewingSteps` 变量**: 完全移除该变量及其约 16 处引用，消除混乱的预览态管理
- **简化三个核心函数**:
  - `renderEraseOverlays()`: 只在 `inFinetune && currentFinetuneStep === 'erase'` 时渲染
  - `updateSubtitlePreview()`: 只在 `inFinetune && currentFinetuneStep === 'subtitle'` 时显示
  - `updateVoicePreview()`: 只在 `inFinetune && currentFinetuneStep === 'voice'` 时显示
  - `updateEraseOverlayVisibility()`: 移除 `unlockedTabs`/`previewingSteps` 依赖
- **`exitFinetune()`**: 退出时直接清空所有 overlay，不再调用 render 函数
- **CSS**: 删除 `.preview-mode` 相关样式（不再需要只读预览外观）
- **清理对话流程**: 移除 `showStepPreBubble`/`showStepResultBubble`/`confirmStep`/`startSubtitleASR`/`startSubtitleUpload` 等函数中的 previewingSteps 操作
- 净减 ~100 行代码
- commit: 9c3267a

## 时间轴 & processingSteps 清理 — 只在精调模式显示

- **核心原则**: 时间轴（timeline）和底部 track 只在精调模式下显示，对话阶段底部保持隐藏
- **删除 `processingSteps` 组件**: 移除 HTML（`<div class="tv2-steps" id="processingSteps">`）+ CSS（`.tv2-steps`/`.pstep` 全套样式 + 动画）+ JS 中约 40 处引用
- **对话流程 track-visible 清理**: 删除 `startStepScan`/`runVoiceGeneration`/`runTranslationScan`/`showStepResultBubble`/`confirmStep` 中所有 `track.classList.add('track-visible')` 调用
- **`exitFinetune()`**: 退出时隐藏所有 tracks（`track.classList.remove('track-visible', ...)`），不再恢复可见
- **`confirmAndFinetune()`**: 删除预设 track-visible（enterFinetune 内部处理）
- **`handleImportedTask()`**: 删除 timeline 显示 + track-visible + processingSteps 代码
- **`showResult()`**: 删除 timeline 显示 + processingSteps 代码
- **`startProcessing()`**: 删除 pstep 动画逻辑，简化为纯进度条
- 净减 ~175 行代码
- commit: 3e8c60e

## 视频译制 V6 — 双路径卡片入口

- **新文件**: `app/translate-v6.html`，基于 v5 创建，不影响之前的页面
- **核心改动**: Landing 页从单一上传区改为双路径卡片入口设计：
  - **路径A「从零开始」**: 点击卡片展开 → 显示 Mode Tabs + 上传区 + 配置面板（复用 v5 逻辑）
  - **路径B「导入已有任务」**: 点击卡片展开 → 显示 MPS/VOD 导入 + 4 种文件导入子类型
- **4 种导入子类型**: 一键译制（视频+Speaker）、擦除（擦除后视频）、配音（Speaker 文件）、智能字幕（SRT/VTT/ASS）
- **文件导入弹窗**: 新增 `#fileImportModal`，支持拖放/点击上传，根据导入类型动态配置标题/提示/accept 属性
- **JS 交互**: 
  - 路径卡片点击 → 隐藏双卡片 → 显示对应展开区（带 `.active` CSS 动画）
  - 返回按钮 → 收起展开区 → 恢复双卡片
  - 导入子类型卡片 → 打开文件导入弹窗 → 确认后设置模式并通过 `_tv2ImportTask` 进入精调
  - URL 参数支持: `?path=new` / `?path=import` 自动展开对应路径
  - 修复 `switchMode()` 不再引用 V6 中已删除的 `#tv2HeroTitle`/`#tv2HeroSubtitle`
- **CSS**: ~350 行新增样式（path-cards、path-expand、import-options、import-mps、file-import-modal 全套组件）
- **VOD picker 和 task import modal**: 按钮 ID 不变，只是位置移到了路径展开区内，功能不受影响
- commit: d952a92

## V6 Landing 重构 — Tab 切换替代层级展开

- **问题**: 双路径卡片点击→隐藏→展开→返回，层级太深，交互繁琐
- **方案**: 改为平级 Tab 切换，两个 Tab 的内容面板直接显示在下方，无需嵌套/返回
- **改动**:
  - **CSS**: `.tv2-path-cards`(双卡片) → `.tv2-path-tabs`(pill Tab 栏)；`.tv2-path-expand` → `.tv2-path-panel`(Tab面板)；新增导入面板两栏样式(`.tv2-import-methods`、ID输入、文件拖放区)
  - **HTML**: Tab 栏 `#tv2PathTabs` 两个按钮(从零开始/导入已有任务)；面板A `#pathPanelNew` 保留 Mode Tabs+上传区+配置面板；面板B `#pathPanelImport` 简化为"输入任务ID" + "上传中间文件"两栏
  - **JS**: `switchPathTab()` Tab 切换函数；删除旧的 `expandPath()`/`collapsePath()` 及返回按钮逻辑；删除旧的 `openFileImportModal()`/`closeFileImportModal()` 弹窗逻辑；新增内联的任务ID导入(`#tv2ImportIdBtn`)和文件上传(`#tv2ImportFileDrop`)交互
  - 删除旧的 `#fileImportModal` 弹窗 HTML 和相关 CSS
- **导入逻辑统一**: 导入中间文件 ≈ 导入任务ID，都是进入精调模式，不再分4个子类型
- 净减 222 行 (374+/596-)
- commit: 391ed4e

## Mode Tabs 融入上传区 — 减少视觉层级

- **问题**: 页面有 Path Tabs + Mode Tabs + 上传区三层，切换点太多，视觉拥挤
- **方案**: Mode Tabs 移入上传区内部作为第一个元素，上传区整体压缩高度
- **改动**:
  - **CSS**: 上传区 padding `64px 40px` → `24px 32px`，删除 `min-height: 320px`，gap `20px` → `14px`；上传图标 `80px` → `48px`，SVG `56px` → `32px`；Mode Tabs margin-bottom → 0，字号缩小
  - **HTML**: Mode Tabs 从 `.tv2-path-panel` 直接子元素移入 `.tv2-upload-zone` 内部，位于 file input 之后
  - **JS**: 上传区点击事件新增 `.tv2-mode-tabs` 排除，防止切换功能时触发文件选择
- commit: 86c909d

## 上传区整体优化 — 去拥挤 + 降级云点播

- **Mode Tabs 移出上传框**: 从 `.tv2-upload-zone` 内部移到 `.tv2-landing-primary-action` 的第一个子元素，上传框回归纯上传职能
- **删除分隔线**: 去掉 "── 或 ──" 分隔线，减少视觉层级
- **云点播降级**: 从带边框/背景的按钮改为纯文本下划线链接（`tv2-vod-pick-inline` → `tv2-vod-pick-link`），字号 0.75rem，颜色 text-faint
- **上传框瘦身**: gap 14px→10px, padding 收紧到 20px 32px 22px, 图标 48px→40px SVG 32px→26px
- **JS 清理**: 移除 mode-tabs 排除判断（不再需要），更新云点播选择器为新 class 名
- 上传框从 5 层元素(tabs/icon/text/divider/button)减为 3 层(icon/text/link)
- commit: 75a25bd

## 导入面板改版 — 三段平铺零下钻

- **问题**: 旧版导入面板分两栏卡片(ID输入+文件拖拽区)，文案不清晰、缺少分类、缺少来源说明；左侧弹窗流程繁琐(选平台→输入ID→查询→确认共6步)
- **方案**: 改为单页三段平铺，所有操作一屏完成，零下钻零弹窗：
  1. **通过任务 ID 导入** — 平台下拉(MPS/云点播) + ID 输入 + 导入按钮，一行搞定
  2. **上传已处理文件** — 4 行文件槽位：📹视频(必须) / 📝字幕(可选,多选) / 🎙配音(可选,多选) / 📋JSON(可选)，每行独立选择器+文件名显示+移除
  3. **FrameX 历史任务** — radio list 直接内联显示3条历史，选中即可导入
- 每个分区标题行右侧加 ⓘ 信息按钮，点击显示 tooltip 说明文件来源
- 左侧"导入已完成任务"按钮改为直接跳转到导入 Tab，不再打开弹窗
- 旧弹窗 `taskImportModal` HTML 保留但不再被触发
- commit: 3a938d1

## V6 正式上线 — 全站导航切换

- **translate-v6.html 正式替代 v5 成为线上版本**，v5 保留作备份
- 修改了 5 个文件中的全部 translate 引用：
  - `create.html`: v5 → v6（首页卡片链接）
  - `index.html`: v4 → v6（侧边栏导航 + 3 个能力卡片，共 4 处）
  - `app.js`: v4 → v6（路由映射 + 上传跳转 + 拖拽跳转 + 侧边栏高亮，共 4 处）
  - `editor.html`: v4 → v6（返回按钮）
  - `editor-v2.html`: v4 → v6（返回按钮）
- commit: cd0e0b5

## 直播剪辑沉浸式重写

- **livestream.html 完全重写**，从旧版 AI 高光风格改为沉浸式手动剪辑工作台
- **布局对齐**: 沿用 translate-v6 / editor-v2 的沉浸式设计系统：
  - 48px topbar（← 返回 + LIVE 标识 + 标题 + 时长 + 导出按钮 + 停止监控）
  - 导出按钮使用 `.ev2-btn-export` 共享样式，位置在右上角
  - 右侧 320px aside 面板（直播片段列表）
  - 底部时间轴区域
- **新功能布局**:
  - 左侧分屏：主直播画面(2/3) + 片段预览区(1/3)
  - 播放控制栏：信号源标识 + 播放按钮 + 时间 + 进度条 + 入点/出点标记 + 音量
  - 操作按钮行：设置入点(绿) + 设置出点(黄) + 生成片段(紫) + 显示时间刻度 + 缩放
  - 帧缩略图时间轴：60帧缩略图 + 时间刻度尺 + 播放头 + 片段标记 + 入出点选区
  - 右侧面板：片段列表(勾选+缩略图+时长+起止+预览+删除) + 底部(清空/全选/批量导出)
- **删除的 AI 功能**: 弹幕热度 HUD、AI 自动标记高光、AI 评分、热度曲线、自动生成弹幕
- **保留/简化**: 弹幕飘屏动画、直播画面 canvas 模拟、主播信息角标
- **首屏简化**: 删除第二屏（场景介绍/数据亮点），只保留链接输入 + 配置
- commit: 6e9bc30
